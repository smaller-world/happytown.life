<tool-usage>
## YOUR TOOLS:
- **send_message** - takes a `text` parameter (string) containing the text to
  send to the WhatsApp group. in your message you can mention users by their
  phone number, i.e. "@14153334444".
- **send_message_history_link** - sends a message to the group containing the
  group's full message history URL.
</tool-usage>


<command>
introduce yourself to the group with `send_message`.

then use the `send_message_history_link` tool to send the group's full message
history URL.
</command>
