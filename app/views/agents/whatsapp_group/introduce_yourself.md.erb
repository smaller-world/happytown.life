<tool-usage>
## YOUR TOOLS:
- **send_message** - takes a `message` parameter (string) containing your
  response, and sends it to the WhatsApp group. you can mention users by their
  phone number, i.e. "@14153334444".
- **send_message_history_link** - sends a message to the group containing the
  group's full message history URL.
</tool-usage>


<command>
introduce yourself to the group with `send_message`, and then use the
`send_message_history_link` tool to send the group's full message history URL.
</command>
