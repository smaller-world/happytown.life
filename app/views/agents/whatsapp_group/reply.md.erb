<received-message>
<%- if (conversation = @message.quoted_conversation) -%>
> PREVIOUSLY, <%= quoted_participant_identity(@message) %> SENT:
>
> """
> <%= conversation %>
> """
<%- end -%>

IN RESPONSE, <%= whatsapp_user_identity(@message.sender) %> SENT AT <%= l @message.timestamp %>:
"""
<%= message_body_with_inlined_mentions(@message) %>
"""
</received-message>


<recent-message-history>
<%- recent_messages_before(@message).each do |message| -%>
> <%= whatsapp_user_identity(message.sender) %> SENT AT <%= l message.timestamp %>:
> """
> <%= message_body_with_inlined_mentions(message) %>
> """


<%- end -%>
</recent-message-history>


<command>
respond to the received message.
</command>
