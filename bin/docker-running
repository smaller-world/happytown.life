#!/usr/bin/env bash

CONTAINER=$1

if [ -f ./tmp/pids/$CONTAINER.cid ]
  if docker inspect -f '{{.State.Running}}' $(cat ./tmp/pids/$CONTAINER.cid) | grep -q true; then
    exit 0
  end

  rm ./tmp/pids/$CONTAINER.cid
end

exit 1
